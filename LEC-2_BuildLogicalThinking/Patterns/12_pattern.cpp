#include <iostream>
using namespace std;
int main(int argc, char const *argv[])
{
    int i, j, n, k;
    cout << "Enter a limit = ";
    cin >> n;
    for (i = 1; i <= n; i++)
    {
        for (j = 1; j <= 2 * n; j++)
        {
            if()
        }
        cout << endl;
    }
}